{
  "name": "soulcloudjs",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run dev:api & bun run dev:broker & wait",
    "dev:api": "bun run --watch packages/api/src/index.ts",
    "dev:broker": "bun run --watch packages/broker/src/index.ts",
    "dev:plugin-manager": "bun run --watch packages/plugin-manager/src/index.ts",
    "dev:plugin": "bun run --watch packages/plugin-runtime/src/index.ts",
    "dev:web": "bun run --cwd packages/web dev",
    "build:web": "bun run --cwd packages/web build",
    "start:api": "bun run packages/api/src/index.ts",
    "start:broker": "bun run packages/broker/src/index.ts",
    "test": "bash scripts/test.sh",
    "typecheck": "tsc --noEmit",
    "db:migrate": "bun run --cwd packages/core db:migrate",
    "db:deploy": "bun run --cwd packages/core db:deploy",
    "db:generate": "bun run --cwd packages/core db:generate"
  },
  "devDependencies": {
    "@types/bun": "1.4.0",
    "@types/node": "^26.2.0",
    "agent-browser": "^0.33.2",
    "bun-types": "1.4.0",
    "mqtt": "^5.15.2",
    "typescript": "^7.0.2"
  }
}
